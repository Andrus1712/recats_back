export interface RoomRequestModel {
    id: string;
    code: string;
    door: number;
    foorl: number;
    description: string;
}

export interface RoomResponseModel {
    id: string;
    code: string;
    door: number;
    foorl: number;
    description: string;
}
